<Page
    x:Class="CharityApp.SuperAdminPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CharityApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="LightGray">
        <Button Content="Wyloguj"
                Click="OnLogoutClick"
                Background="Transparent"
                Foreground="DarkSlateGray"
                FontSize="16"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="10"/>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="Panel SuperAdmina"
                       FontSize="32"
                       FontWeight="SemiBold"
                       Foreground="DarkSlateGray"
                       TextAlignment="Center"
                       Margin="0,20,0,10"/>

            <Button Content="Dodaj admina"
                    Click="OnAddAdminClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="50"
                    FontSize="20"/>

            <Button Content="Lista adminów"
                    Click="OnViewAdminsClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="50"
                    FontSize="20"/>

            <Button Content="Wprowadź przelew"
                    Click="OnAddTransferClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="50"
                    FontSize="20"/>

            <Button Content="Lista celów"
                    Click="OnListGoalsClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="50"
                    FontSize="20"/>
            
            <Button Content="Dodaj cel"
                    Click="OnAddGoalClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="50"
                    FontSize="20"/>
        </StackPanel>

        <TextBlock x:Name="FundsTextBlock"
                   FontSize="25"
                   Foreground="DarkSlateGray"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="10">
            <Run Text="Zebrano: "/>
            <Run Text="{x:Bind TotalAmount}"/>
            <Run Text=" PLN"/>
        </TextBlock>
    </Grid>
</Page>