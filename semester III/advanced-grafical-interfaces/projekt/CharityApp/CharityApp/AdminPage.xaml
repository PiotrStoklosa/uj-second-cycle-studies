<Page
    x:Class="CharityApp.AdminPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CharityApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="LightGray">
        <Button Content="Wyloguj"
                Click="OnLogoutClick"
                Background="Transparent"
                Foreground="DarkSlateGray"
                FontSize="16"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="10"/>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="Panel Administratora"
                       FontSize="32"
                       FontWeight="SemiBold"
                       Foreground="DarkSlateGray"
                       TextAlignment="Center"
                       Margin="0,20,0,10"/>

            <Button Content="Dodaj wolontariusza"
                    Click="OnAddVolunteerClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="50"
                    FontSize="20"/>

            <Button Content="Rozlicz puszkę"
                    Click="OnSubmitAmountClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="50"
                    FontSize="20"/>
        </StackPanel>
    </Grid>
</Page>
