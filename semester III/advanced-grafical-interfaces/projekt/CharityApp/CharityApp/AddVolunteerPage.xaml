<Page
    x:Class="CharityApp.AddVolunteerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CharityApp">

    <Grid Background="White">
        <Button Content="← Powrót"
                Click="OnBackButtonClick"
                Background="Transparent"
                Foreground="DarkSlateGray"
                FontSize="20"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="10"/>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="15" Width="300">
            <TextBlock Text="Dodaj wolontariusza"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="DarkSlateGray"
                       TextAlignment="Center"/>

            <TextBox x:Name="NameTextBox" PlaceholderText="Imię i nazwisko" Width="300"/>
            <TextBox x:Name="LocationTextBox" PlaceholderText="Lokalizacja" Width="300"/>
            <TextBox x:Name="UsernameTextBox" PlaceholderText="Użytkownik" Width="300"/>
            <TextBox x:Name="PasswordTextBox" PlaceholderText="Hasło" Width="300"/>
            <DatePicker x:Name="DateBox" Width="300"/>
            <TimePicker x:Name="TimeBox" ClockIdentifier="24HourClock" Width="300"/>

            <Button Content="Dodaj"
                    Click="OnAddClick"
                    Background="DarkSlateGray"
                    Foreground="White"
                    Width="300"
                    Height="40"/>

            <TextBlock x:Name="MessageTextBlock" Text="" Foreground="Red" FontSize="18" TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>