<Page
    x:Class="BattleshipGame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BattleshipGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Plansza gracza -->
        <Grid x:ConnectionId='2' x:Name="PlayerBoard" Grid.Column="0" Margin="10" Background="LightGray">
            <!-- Tutaj będą kafelki -->
        </Grid>

        <!-- Plansza przeciwnika -->
        <Grid x:ConnectionId='3' x:Name="OpponentBoard" Grid.Column="1" Margin="10" Background="Gray">
            <!-- Tutaj będą kafelki -->
        </Grid>

        <!-- Przyciski -->
        <Button x:ConnectionId='4' x:Name="ConfirmShipsButton" 
                Grid.Row="1" Grid.ColumnSpan="2"
                Content="Zatwierdź statki" 
                Margin="10" 
                HorizontalAlignment="Center"
                                              />

        <!-- Tekst "Twoja tura" -->
        <TextBlock x:ConnectionId='5' x:Name="TurnText" 
                   Grid.Row="1" Grid.ColumnSpan="2" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   FontSize="24" 
                   Visibility="Collapsed" />
    </Grid>
</Page>

